module.exports = {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
      // Assuming the instruction to "add artico font" implies a need for custom CSS handling or font inclusion via PostCSS
      // However, PostCSS itself doesn't directly handle font imports. Fonts are usually handled in CSS or through Tailwind's theme.extend in tailwind.config.js
      // If there's a specific PostCSS plugin for handling fonts in a unique way, it would be specified here, but without a known PostCSS plugin for "artico font", this is a placeholder comment.
    },
  };  
